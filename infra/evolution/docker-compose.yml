services:
  organiza-club-evolution-api:
    image: evoapicloud/evolution-api:v2.3.3
    container_name: organiza-club-evolution-api
    restart: unless-stopped
    depends_on:
      - organiza-club-evolution-postgres
      - organiza-club-evolution-redis
    environment:
      SERVER_URL: ${EVOLUTION_SERVER_URL}
      AUTHENTICATION_API_KEY: ${EVOLUTION_API_KEY}
      DATABASE_ENABLED: "true"
      DATABASE_PROVIDER: postgresql
      DATABASE_CONNECTION_URI: postgresql://${EVOLUTION_DB_USER}:${EVOLUTION_DB_PASSWORD}@organiza-club-evolution-postgres:5432/${EVOLUTION_DB_NAME}
      CACHE_REDIS_ENABLED: "true"
      CACHE_REDIS_URI: redis://organiza-club-evolution-redis:6379/6
      LOG_LEVEL: ERROR
    ports:
      - "127.0.0.1:${EVOLUTION_PORT:-8081}:8080"
    networks:
      - organiza_club_net

  organiza-club-evolution-postgres:
    image: postgres:16.9-alpine
    container_name: organiza-club-evolution-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${EVOLUTION_DB_USER}
      POSTGRES_PASSWORD: ${EVOLUTION_DB_PASSWORD}
      POSTGRES_DB: ${EVOLUTION_DB_NAME}
    volumes:
      - evolution_postgres_data:/var/lib/postgresql/data
    networks:
      - organiza_club_net
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${EVOLUTION_DB_USER} -d ${EVOLUTION_DB_NAME}"]
      interval: 10s
      timeout: 5s
      retries: 10

  organiza-club-evolution-redis:
    image: redis:7.4.4-alpine
    container_name: organiza-club-evolution-redis
    restart: unless-stopped
    command: ["redis-server", "--appendonly", "yes"]
    volumes:
      - evolution_redis_data:/data
    networks:
      - organiza_club_net

volumes:
  evolution_postgres_data:
  evolution_redis_data:

networks:
  organiza_club_net:
    external: true
